<!DOCTYPE html>
<html>
  <head>
    <title>Epresto</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_include_tag "application", "data-turbo-track": "reload", defer: true %>
  </head>

  <body>
    <div class="grid grid-cols-5 gap-4 pr-3 bg-dark vh-100">
      <div class="col-span-1 ps-0 position-fixed">
        <%= render "sidebar" %>
      </div>

      <div class='col-span-4'>
        <%= render 'flash' %>
        <%= yield %>
      </div>
  </body>
</html>
